/**
 * Portal Loading State
 * Displays a skeleton loader while portal content is being fetched
 */

import { PageSkeleton } from '@/components/ui/skeleton'

export default function PortalLoading() {
  return <PageSkeleton />
}
