// Superseded: EBT types are now generated into <repo>/types/supabase.ts (public + ebt).
// Kept as a re-export so any lingering `@/lib/ebt/database.types` imports resolve.
export type { Json, Database } from '@/types/supabase'
