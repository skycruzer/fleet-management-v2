# E2E Test Environment Variables
# Copy this file to .env.test.local and fill in your test credentials
# NEVER commit .env.test.local to version control

# Admin Portal Test Credentials (Supabase Auth)
# Create a dedicated test admin account for E2E tests
TEST_ADMIN_EMAIL=test-admin@example.com
TEST_ADMIN_PASSWORD=your-secure-test-password

# Pilot Portal Test Credentials (Custom Auth via an_users table)
# Create a dedicated test pilot account for E2E tests
TEST_PILOT_EMAIL=test-pilot@example.com
TEST_PILOT_PASSWORD=your-secure-test-password

# Legacy test credentials (backward compatibility)
TEST_USER_EMAIL=test-admin@example.com
TEST_USER_PASSWORD=your-secure-test-password

# Test environment URL
PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000

# IMPORTANT SECURITY NOTES:
# 1. Use DEDICATED test accounts - never use production credentials
# 2. Use WEAK passwords for test accounts (they're not protecting real data)
# 3. Ensure test accounts have limited permissions
# 4. Never commit this file with real credentials
