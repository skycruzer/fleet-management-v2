# Supabase Configuration
# Get these values from your Supabase project settings
# https://app.supabase.com/project/YOUR_PROJECT_ID/settings/api

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="Fleet Management V2"
NEXT_PUBLIC_APP_VERSION="0.1.0"

# Optional: Service Role Key (NEVER expose this to the client!)
# Only use this in server-side code or scripts
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Upstash Redis Configuration (for rate limiting)
# Get these values from your Upstash Redis dashboard
# https://console.upstash.com/redis
# Free tier: 10,000 requests/day, 256MB storage

UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here

# Email Service (Resend)
# Required for:
#   - Pilot registration emails
#   - Certification expiry alerts (automated daily)
#   - Password reset emails
#   - Leave request approval/denial notifications
#   - Flight request approval/denial notifications
# Get your API key from: https://resend.com/api-keys
# Domain must be verified in Resend dashboard

RESEND_API_KEY=your-resend-api-key-here
RESEND_FROM_EMAIL=Fleet Management <noreply@pxb767office.app>
SUPPORT_EMAIL=support@yourcompany.com

# Cron Job Security
# Generate a strong random secret for Vercel cron jobs
# Use a password generator with at least 32 characters
# IMPORTANT: Set this same value in Vercel Environment Variables

CRON_SECRET=generate-a-strong-random-secret-here

# Error Monitoring (Optional - Sentry)
# To enable: npm install @sentry/nextjs
# Get DSN from: https://sentry.io/settings/projects/YOUR_PROJECT/keys/
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Current Roster Period (for calculations)
NEXT_PUBLIC_CURRENT_ROSTER=RP12/2025
NEXT_PUBLIC_ROSTER_END_DATE=2025-11-07

# =============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# =============================================================================
#
# Before deploying to Vercel:
#
# 1. Verify Resend Domain
#    - Go to https://resend.com/domains
#    - Confirm pxb767office.app is verified (green checkmark)
#
# 2. Configure Vercel Environment Variables
#    - Go to: Vercel Dashboard → Project → Settings → Environment Variables
#    - Add all variables from this file
#    - Set NEXT_PUBLIC_APP_URL to your production URL
#    - Generate a NEW strong CRON_SECRET for production
#
# 3. Deploy Application
#    - Push to GitHub (if using Git integration)
#    - Or run: vercel --prod
#
# 4. Verify Cron Job
#    - Check: Vercel Dashboard → Deployments → Cron Jobs
#    - Verify certification-expiry-alerts is scheduled for 0 6 * * *
#
# 5. Test Email Delivery
#    - Wait for first cron run (6:00 AM next day)
#    - Or manually trigger using test script
#    - Check pilot email inboxes
